<% unless @productions.empty? %>

    <ul class="th">
      <li>Date</li>
      <li>Name</li>
      <li>Sum</li>
      <li>Price</li>
      <li></li>
      <li></li>
      <li></li>
    </ul>

  <% @productions.each do |production| %>
    <ul>
      <li><%= production.when %></li>
      <li><%= Worker.find(production.who_id).name %></li>
      <li><%= Product.find(production.what_id).name %></li>
      <li><%= production.number %></li>
      <li><%= link_to "Delete", {:controller=>"productions", :action=>"delete",:id=>production}, :class=>"delete"%></li>
      <li>Edit</li>
    </ul>
  <% end %>

  <%= paginate @productions %>

<% end %>


